[build]
  publish = "frontend/"

# Proxy all /api/* calls to the Render backend — eliminates CORS for REST endpoints
[[redirects]]
  from   = "/api/*"
  to     = "https://tradeski.onrender.com/:splat"
  status = 200
  force  = true

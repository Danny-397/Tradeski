[build]
builder = "nixpacks"

[deploy]
startCommand = "PYTHONPATH=/app python \"Plotly dashboard/app.py\""
healthcheckPath = "/market_status"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
